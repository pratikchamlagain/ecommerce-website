import PageShell from "../components/common/PageShell";

export default function CartPage() {
  return (
    <PageShell title="Cart">
      <p>Cart page placeholder. Day 2: React cart state + API sync.</p>
    </PageShell>
  );
}
